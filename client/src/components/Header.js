import { PageHeader } from 'antd';

const Header = () => {
    return (
        <PageHeader
            className="site-page-header"
            title="GoThere"
        />
    );
};

export default Header;